RosAtomInfo - сервис для информирования сотрудников на рабочих станциях

INSTALLATION

1) create user for service
adduser rosatominfo
usermode -aG sudo rosatominfo

2) install need things
curl -sSL https://install.python-poetry.org | python3 -
export PATH=$PATH:$HOME/.poetry/bin


2) install dependencies

